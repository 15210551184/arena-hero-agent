[build-system]
requires = ["setuptools==83.0.0", "wheel==0.46.2"]
build-backend = "setuptools.build_meta"

[project]
name = "arena-hero-agent"
version = "0.1.0"
description = "A deterministic, resource-first Arena Hero agent with hierarchical threat response."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE"]
dependencies = [
    "arena-hero @ https://github.com/arena-hero/arena-hero-python/archive/423d252adcca439669adb3e7b04252e53b4430bd.tar.gz",
    "httpx==0.28.1",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Private :: Do Not Upload",
]

[project.urls]
Homepage = "https://github.com/Drew-Z/arena-hero-agent"
Documentation = "https://github.com/Drew-Z/arena-hero-agent#readme"
Issues = "https://github.com/Drew-Z/arena-hero-agent/issues"
Source = "https://github.com/Drew-Z/arena-hero-agent"

[project.scripts]
arena-hero-agent = "arena_farmer:main"
arena-hero-supervisor = "arena_supervisor:main"
arena-hero-optimizer = "arena_optimizer:main"
arena-hero-version-monitor = "arena_version_monitor:main"
arena-hero-health = "arena_health:main"

[tool.setuptools]
py-modules = [
    "arena_farmer",
    "arena_optimizer",
    "arena_supervisor",
    "arena_version_monitor",
    "arena_health",
]

[tool.setuptools.packages.find]
include = ["dashboard*"]

[tool.setuptools.package-data]
dashboard = ["index.html"]
